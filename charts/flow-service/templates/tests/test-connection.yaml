apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flow-service.fullname" . }}-test-connection"
  labels:
    {{- include "flow-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flow-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
